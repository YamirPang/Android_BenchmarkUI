<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <com.zealp.benchmark_ui.widget.preview.view.SmoothImageView
        android:id="@+id/photoView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/transparent" />

    <!--<ProgressBar-->
    <!--android:id="@+id/loading"-->
    <!--style="@android:style/Widget.DeviceDefault.Light.ProgressBar"-->
    <!--android:layout_width="65dp"-->
    <!--android:layout_height="65dp"-->
    <!--android:layout_centerInParent="true"-->
    <!--android:visibility="visible" />-->

    <com.zealp.benchmark_ui.widget.loading.ChrysanthemumLoadingView
        android:id="@+id/loading"
        android:layout_width="65dp"
        android:layout_height="65dp"
        android:layout_centerInParent="true"
        app:chrysanthemum_turn_view_color="@color/base_color_white"
        app:chrysanthemum_turn_view_size="65dp"
        android:visibility="visible" />

    <ImageView
        android:id="@+id/btnVideo"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_centerInParent="true"
        android:alpha="0"
        android:src="@drawable/benchmark_ui_icon_video"
        android:visibility="gone" />

</RelativeLayout>